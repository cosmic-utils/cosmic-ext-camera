[package]
name = "camera"
version = "1.2.2"
edition = "2024"
license = "GPL-3.0-only"
description = "Modern camera app for Linux desktops and phones"
repository = "https://github.com/cosmic-utils/cosmic-ext-camera"
authors = ["Frederic Laing <frederic.laing.development@gmail.com>"]

[dependencies]
# Pure-Rust client for the PulseAudio wire protocol. Used to enumerate audio
# sources and to boost the selected source to 100% before recording — avoids
# shelling out to `pactl`/`pw-dump`, which are not shipped inside the
# `org.freedesktop.Platform` flatpak runtime.
pulseaudio = "0.3"
futures = "0.3.32"
i18n-embed = { version = "0.16.0", features = [
    "fluent-system",
    "desktop-requester",
] }
i18n-embed-fl = "0.10.0"
open = "5.3.5"
pollster = "1.0.1"
rust-embed = "8.11.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
tokio = { version = "1.52.3", features = ["full"] }
tracing = "0.1.44"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
gstreamer = "0.25.2"
gstreamer-app = "0.25.2"
gstreamer-video = "0.25.2"
image = "0.25.10"
chrono = "0.4.45"
ctrlc = "3.5.2"
bytemuck = { version = "1.25.0", features = ["derive"] }
dirs = "6.0.0"
rqrr = "0.10.1"
rfd = "0.17.2"
zbus = "5.16.0"
# XDG portal integration for color-scheme detection on non-COSMIC desktops
ashpd = { version = "0.13.11", default-features = false, features = ["tokio", "settings"] }
uuid = { version = "1.23.2", features = ["v4"] }
clap = { version = "4.6.1", features = ["derive"] }
ratatui = "0.30.1"
crossterm = "0.29.0"
libc = "0.2.186"
dng = "1.6.0"
async-stream = "0.3.6"
# Native libcamera bindings for direct camera access (bypasses GStreamer)
libcamera = "0.7.0"
drm-fourcc = "2.2.0"
turbojpeg = { version = "1.5.1", default-features = false, features = ["cmake"] }

[dependencies.iced_core]
git = "https://github.com/pop-os/libcosmic.git"

[dependencies.iced_wgpu]
git = "https://github.com/pop-os/libcosmic.git"

[dependencies.iced_futures]
git = "https://github.com/pop-os/libcosmic.git"

[dependencies.libcosmic]
git = "https://github.com/pop-os/libcosmic.git"
# See https://github.com/pop-os/libcosmic/blob/master/Cargo.toml for available features.
features = [
    # Accessibility support
    "a11y",
    # About widget for the app
    "about",
    # Uses cosmic-settings-daemon to watch for config file changes
    "dbus-config",
    # Support creating additional application windows.
    "multi-window",
    # On app startup, focuses an existing instance if the app is already open
    "single-instance",
    # Uses tokio as the executor for the runtime
    "tokio",
    # Windowing support for X11, Windows, Mac, & Redox
    "winit",
    # Add Wayland support to winit
    "wayland",
    # GPU-accelerated rendering
    "wgpu",
    # XDG portal integration for Flatpak theme detection
    "xdg-portal",
]

[profile.release]
lto = "fat"
codegen-units = 1
strip = false
debug = 1

[profile.release-fast]
inherits = "release"
lto = false
codegen-units = 16
debug = 0
opt-level = 1

[dev-dependencies]
naga = { version = "30.0.0", features = ["wgsl-in"] }
